import type { AsyncStorageWrapper } from 'next/dist/server/async-storage/async-storage-wrapperstorage/async-storage-wrapper';
import type { StaticGenerationStore } from 'next/dist/client/components/static-generation-async-storagerage';
import type { IncrementalCache } from 'next/dist/server/lib/incremental-cacheremental-cache';
export type StaticGenerationContext = {
    pathname: string;
    renderOpts: {
        originalPathname?: string;
        incrementalCache?: IncrementalCache;
        supportsDynamicHTML: boolean;
        isRevalidate?: boolean;
        isOnDemandRevalidate?: boolean;
        isBot?: boolean;
        nextExport?: boolean;
        fetchCache?: StaticGenerationStore['fetchCache'];
        isDraftMode?: boolean;
    };
};
export declare const StaticGenerationAsyncStorageWrapper: AsyncStorageWrapper<StaticGenerationStore, StaticGenerationContext>;
