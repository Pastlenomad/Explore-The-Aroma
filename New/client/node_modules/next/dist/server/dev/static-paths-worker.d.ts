import type { NextConfigComplete } from 'next/dist/server/config-shared/config-shared';
import 'next/dist/server/require-hookr/require-hook';
import 'next/dist/server/node-polyfill-fetchpolyfill-fetch';
import 'next/dist/server/node-environmentde-environment';
import { IncrementalCache } from 'next/dist/server/lib/incremental-cacheremental-cache';
type RuntimeConfig = any;
export declare function loadStaticPaths({ distDir, pathname, config, httpAgentOptions, locales, defaultLocale, isAppPath, originalAppPath, isrFlushToDisk, fetchCacheKeyPrefix, maxMemoryCacheSize, requestHeaders, incrementalCacheHandlerPath, }: {
    distDir: string;
    pathname: string;
    config: RuntimeConfig;
    httpAgentOptions: NextConfigComplete['httpAgentOptions'];
    locales?: string[];
    defaultLocale?: string;
    isAppPath?: boolean;
    originalAppPath?: string;
    isrFlushToDisk?: boolean;
    fetchCacheKeyPrefix?: string;
    maxMemoryCacheSize?: number;
    requestHeaders: IncrementalCache['requestHeaders'];
    incrementalCacheHandlerPath?: string;
}): Promise<{
    paths?: string[];
    encodedPaths?: string[];
    fallback?: boolean | 'blocking';
}>;
export {};
